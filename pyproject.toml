[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mini-sglang-metax"
version = "0.1.0a1"
description = "Correctness-first Mini-SGLang port for MetaX MACA accelerators."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
    "accelerate",
    "msgpack",
    "modelscope",
    "torch>=2.4,<2.11",
    "transformers>=4.56.0,<=4.57.3",
    "pyzmq",
    "uvicorn",
    "fastapi",
    "prompt_toolkit",
    "openai",
    # apache-tvm-ffi drives the AOT-compiled CPU helpers in
    # ``minisgl.kernel.{radix,tensor}`` — ``fast_compare_key`` is invoked by
    # ``RadixPrefixCache._tree_walk`` on every prefix match, so it is
    # required on ALL devices (CUDA, Ascend NPU, CPU). Keeping it in the
    # ``[cuda]`` extra caused clean Ascend installs to fail with
    # ``ModuleNotFoundError: No module named 'tvm_ffi'`` at the first
    # prefill scheduling call (Gate 2.1e). Membership belongs in the base
    # dependency list, not in a device-specific extra.
    "apache-tvm-ffi>=0.1.4",
]

[project.urls]
Homepage = "https://github.com/sxvvv/mini-sglang-metax"
Repository = "https://github.com/sxvvv/mini-sglang-metax"
Issues = "https://github.com/sxvvv/mini-sglang-metax/issues"
Upstream = "https://github.com/sgl-project/mini-sglang"
MetaX = "https://github.com/MetaX-MACA"

[project.optional-dependencies]
cuda = [
    "flashinfer-python>=0.5.3",
    "sgl_kernel>=0.3.17.post1",
    "quack-kernels",
]
dev = [
    "pytest>=6.0",
    "pytest-cov>=2.0",
    "black>=22.0",
    "flake8>=4.0",
    "mypy>=0.950",
    "pre-commit>=3.0.0",
    "ruff>=0.11.0",
    "matplotlib>=3.10.5",
    "pyarrow",
]

[tool.setuptools]
include-package-data = true
package-dir = {"" = "python"}

[tool.setuptools.packages.find]
where = ["python"]

[tool.setuptools.package-data]
"*" = ["csrc/**/*"]

[tool.black]
line-length = 100
target-version = ['py310']
include = '\.pyi?$'
extend-exclude = '''
/(
  # directories
  \.eggs
  | \.git
  | \.hg
  | \.mypy_cache
  | \.tox
  | \.venv
  | build
  | dist
)/
'''

[tool.ruff]
target-version = "py310"
line-length = 100
lint.select = [
    "W",  # pycodestyle warnings
    "F",  # pyflakes
    "I",  # isort
    "C4", # flake8-comprehensions
]
lint.ignore = [
    "E501",  # line too long, handled by black
    "B008",  # do not perform function calls in argument defaults
    "C901",  # too complex
]
exclude = [
    ".git",
    ".venv",
    "build",
    "dist",
    "__pycache__",
    ".pytest_cache",
    ".mypy_cache",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py", "*_test.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "--strict-markers",
    "--strict-config",
    "--cov=minisgl",
    "--cov-report=term-missing",
    "--cov-report=html",
]

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
disallow_untyped_decorators = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
warn_unreachable = true
strict_equality = true

[[tool.mypy.overrides]]
module = [
    "torch.*",
    "transformers.*",
    "datasets.*",
]
ignore_missing_imports = true
